```{r}
# Example usage:
chart <-make_indicator_chart_lines(
   data       = raw_norm,
   indicator  = "TRIP_VOLUME",
   region     = region_name,
   date_from  = date_from,
   date_to    = date_to,
   title      = "Wait Time (minutes)"
 )
chart
```